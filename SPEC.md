# 노마드 코리아 웹사이트 개선 실행 계획

## 프로젝트 오버뷰

현재 웹사이트를 개선하기 위해 5개의 Phase로 나누어 점진적으로 프로젝트를 개선합니다. 각 Phase는 독립적으로 실행 가능하며, 데이터베이스 없이 가짜 데이터로 진행합니다.

## Phase 목록

- [x] Phase 1: 네비게이션 단순화 및 불필요한 섹션 제거
- [x] Phase 2: 평가 시스템 변경 (별점 → 좋아요/싫어요)
- [x] Phase 3: 필터 시스템 재설계
- [ ] Phase 4: 카드 구조 변경 및 가짜 데이터 업데이트
- [ ] Phase 5: 도시 리스트 섹션 개선

---

## Phase 1: 네비게이션 단순화 및 불필요한 섹션 제거

### 개선 사항 오버뷰
네비게이션 메뉴를 인증/프로필 관련 기능만 남기고 단순화하며, 홈페이지에서 불필요한 섹션들을 제거하여 핵심 기능에 집중합니다. 현재 도시 비교, 계절별 추천, 리뷰, 프로그램 관련 섹션을 모두 제거하고 도시 탐색과 필터링 기능만 유지합니다.

### 수정/개선 기능
- [x] GNB 데스크톱 메뉴(`src/components/layout/gnb/GNBDesktopMenu.tsx`)에서 모든 네비게이션 링크 제거
- [x] GNB 모바일 메뉴(`src/components/layout/gnb/GNBMobileMenu.tsx`)에서 모든 네비게이션 링크 제거
- [x] 홈페이지(`src/app/page.tsx`)에서 다음 섹션 제거:
  - [x] `CityCompareSection` (도시 비교)
  - [x] `SeasonalSection` (계절별 추천)
  - [x] `ReviewsSection` (리뷰)
  - [x] `ProgramsSection` (워케이션 프로그램)
  - [x] `CTASection` (CTA)
- [x] `SocialProofBar` 컴포넌트 유지 (통계 정보 표시)
- [x] `HeroSection`, `TopCitiesSection`, `CityFilterSection`만 남기기

### 작업 완료 후 검증 사항
- [x] 홈페이지에 HeroSection, SocialProofBar, TopCitiesSection, CityFilterSection만 표시되는지 확인
- [x] GNB에 로고, 프로필/인증 버튼만 표시되고 다른 메뉴 링크는 없는지 확인
- [x] 데스크톱과 모바일 뷰 모두에서 네비게이션이 정상 작동하는지 확인
- [x] 페이지가 에러 없이 렌더링되는지 확인

---

## Phase 2: 평가 시스템 변경 (별점 → 좋아요/싫어요)

### 개선 사항 오버뷰
기존의 score 기반 별점 시스템을 좋아요/싫어요 버튼 시스템으로 교체합니다. 각 도시마다 좋아요/싫어요 카운트를 표시하고, 사용자가 버튼을 클릭하면 상태가 토글되며 아이콘 색상과 숫자가 동적으로 변경됩니다. 아직 데이터베이스를 사용하지 않으므로 클라이언트 상태 관리로 구현합니다.

### 수정/개선 기능
- [x] City 타입(`src/types/city.ts`)에 다음 필드 추가:
  - [x] `likes: number` (좋아요 수)
  - [x] `dislikes: number` (싫어요 수)
- [x] City 타입에서 `score` 필드 제거
- [x] 도시 데이터(`src/data/cities.ts`)에 각 도시별 `likes`, `dislikes` 초기값 추가
- [x] CityRankCard 컴포넌트(`src/components/sections/top-cities/CityRankCard.tsx`) 수정:
  - [x] 종합점수 및 Progress bar 제거
  - [x] 좋아요/싫어요 버튼 UI 추가 (ThumbsUp/ThumbsDown 아이콘)
  - [x] 클라이언트 컴포넌트로 변경 (`'use client'`)
  - [x] useState로 좋아요 상태 관리 (`liked`, `disliked`, `likesCount`, `dislikesCount`)
  - [x] 좋아요 버튼 클릭 시:
    - [x] 이미 좋아요 상태면 취소 (회색으로, 카운트 -1)
    - [x] 싫어요 상태였으면 싫어요 취소 후 좋아요 활성화 (카운트 조정)
    - [x] 아무 상태도 아니면 좋아요 활성화 (초록색, 카운트 +1)
  - [x] 싫어요 버튼도 동일한 로직으로 구현 (빨간색)
- [x] CityRankCard에서 `rankBg` 스타일 유지 (1,2,3위 배경색)
- [x] TOP_CITIES_THIS_MONTH 로직은 일단 유지 (Phase 5에서 변경 예정)

### 작업 완료 후 검증 사항
- [x] 각 도시 카드에 좋아요/싫어요 버튼이 표시되는지 확인
- [x] 좋아요 버튼 클릭 시 아이콘이 회색 → 초록색으로 변경되고 숫자가 증가하는지 확인
- [x] 싫어요 버튼 클릭 시 아이콘이 회색 → 빨간색으로 변경되고 숫자가 증가하는지 확인
- [x] 좋아요 상태에서 좋아요 버튼 재클릭 시 취소되는지 확인 (회색, 숫자 감소)
- [x] 좋아요 상태에서 싫어요 버튼 클릭 시 좋아요 취소 → 싫어요 활성화되는지 확인
- [x] 반대 경우도 동일하게 작동하는지 확인
- [x] TypeScript 오류 없이 빌드되는지 확인

---

## Phase 3: 필터 시스템 재설계

### 개선 사항 오버뷰
기존 필터 옵션을 완전히 재설계합니다. 예산은 3단계로 단순화하고, 지역 필터를 추가하며, 자연환경은 환경 성향(자연친화, 도심선호, 카페작업, 코워킹 필수)으로 변경합니다. 접근성 필터는 제거하고 계절 필터는 유지합니다.

### 수정/개선 기능
- [x] City 타입(`src/types/city.ts`) 수정:
  - [x] `NatureType` 제거하고 `EnvironmentType` 추가: `'nature' | 'urban' | 'cafe' | 'coworking'`
  - [x] `AccessibilityType` 제거
  - [x] `Region` 타입 추가: `'수도권' | '경상도' | '전라도' | '강원도' | '제주도' | '충청도'`
  - [x] City 인터페이스에 `region` 필드를 `Region` 타입으로 변경
  - [x] `nature` 필드를 `environment: EnvironmentType`으로 변경
  - [x] `accessibility` 필드 제거
  - [x] `budgetLevel` 필드 추가: `'100만원' | '100~200만원' | '200만원'`
- [x] CityFilterSection(`src/components/sections/city-filter/CityFilterSection.tsx`) 수정:
  - [x] 예산 슬라이더 제거, 라디오 버튼으로 변경 (100만원, 100~200만원, 200만원 이상)
  - [x] 지역 필터 추가 (라디오 버튼: 전체, 수도권, 경상도, 전라도, 강원도, 제주도, 충청도)
  - [x] 자연환경 옵션을 환경 성향으로 변경:
    - [x] 자연친화 (🌿 nature)
    - [x] 도심선호 (🏙️ urban)
    - [x] 카페작업 (☕ cafe)
    - [x] 코워킹 필수 (🏢 coworking)
  - [x] 접근성 필터 제거
  - [x] 계절 필터 유지 (상관없음, 봄, 여름, 가을, 겨울)
  - [x] 코워킹 체크박스 제거 (환경 성향에 통합)
  - [x] 필터 로직 업데이트하여 새로운 필터 조건에 맞게 수정

### 작업 완료 후 검증 사항
- [x] 필터 섹션에 4가지 필터(예산, 지역, 환경, 계절)가 표시되는지 확인
- [x] 예산 필터가 라디오 버튼으로 표시되고 선택 가능한지 확인
- [x] 지역 필터가 정상 작동하는지 확인
- [x] 환경 필터의 4가지 옵션이 표시되고 선택 가능한지 확인
- [x] 계절 필터가 정상 작동하는지 확인
- [x] 필터 조건에 맞는 도시 개수가 정확히 계산되는지 확인
- [x] TypeScript 오류 없이 빌드되는지 확인

---

## Phase 4: 카드 구조 변경 및 가짜 데이터 업데이트

### 개선 사항 오버뷰
도시 카드의 표시 정보를 변경하여 필터 기준(예산, 지역, 환경, 최고 계절)을 key-value 형태로 보여줍니다. 기존의 인터넷 속도, 월 생활비, 코워킹스페이스 수, 서울 접근성 정보를 제거하고 필터 정보로 대체합니다. 가짜 데이터도 각 도시별로 필터 요소를 골고루 분배하여 업데이트합니다.

### 수정/개선 기능
- [ ] 도시 데이터(`src/data/cities.ts`) 업데이트:
  - [ ] 각 도시에 `budgetLevel`, `region`, `environment` 필드 추가
  - [ ] 각 지역별로 최소 1개 도시씩 포함되도록 도시 추가/수정:
    - [ ] 수도권: 인천, 수원 등
    - [ ] 경상도: 부산, 경주, 통영 등
    - [ ] 전라도: 전주, 여수, 순천 등
    - [ ] 강원도: 강릉, 춘천, 속초 등
    - [ ] 제주도: 제주시, 서귀포 등
    - [ ] 충청도: 대전, 공주, 천안 등
  - [ ] 각 환경 타입별로 도시 분배 (자연친화, 도심선호, 카페작업, 코워킹 필수)
  - [ ] 각 예산 레벨별로 도시 분배
  - [ ] 각 계절별로 추천 도시 분배
  - [ ] 불필요한 필드 제거: `internetSpeed`, `coworkingCount`, `seoulAccess`, `hasCoworking`, `accessibility`
- [ ] CityRankCard 컴포넌트(`src/components/sections/top-cities/CityRankCard.tsx`) 수정:
  - [ ] Key stats 그리드 영역을 필터 정보로 변경:
    - [ ] 예산: `budgetLevel` 표시
    - [ ] 지역: `region` 표시
    - [ ] 환경: `environment` 매핑하여 표시 (자연친화/도심선호/카페작업/코워킹 필수)
    - [ ] 최고 계절: `bestSeasons` 배열을 문자열로 변환하여 표시 (예: "봄, 가을")
  - [ ] 각 항목에 적절한 아이콘 추가

### 작업 완료 후 검증 사항
- [ ] 모든 도시 데이터가 새로운 필드를 포함하는지 확인
- [ ] 각 지역별로 최소 1개 이상의 도시가 있는지 확인
- [ ] 도시 카드에 예산, 지역, 환경, 최고 계절 정보가 표시되는지 확인
- [ ] 각 정보가 올바른 형식으로 표시되는지 확인 (아이콘, 레이블)
- [ ] TypeScript 오류 없이 빌드되는지 확인
- [ ] 필터 기능이 새로운 데이터 구조와 호환되는지 확인

---

## Phase 5: 도시 리스트 섹션 개선

### 개선 사항 오버뷰
"TOP 3 워케이션 도시" 섹션을 "도시 리스트"로 변경하고, 모든 도시를 좋아요 순서대로 정렬하여 표시합니다. 순위 뱃지(🥇🥈🥉)와 특별 배경색은 제거하고, 그리드 레이아웃으로 모든 도시를 동일하게 표시합니다. "전체 도시 순위 보기" 링크도 제거합니다.

### 수정/개선 기능
- [ ] TopCitiesSection 컴포넌트(`src/components/sections/top-cities/TopCitiesSection.tsx`) 수정:
  - [ ] 클라이언트 컴포넌트로 변경 (`'use client'`)
  - [ ] 섹션 제목을 "도시 리스트"로 변경
  - [ ] 부제목 수정 (좋아요 기반 정렬 설명)
  - [ ] `TOP_CITIES_THIS_MONTH` 대신 전체 `CITIES` 배열 사용
  - [ ] `likes` 순으로 내림차순 정렬 (`useMemo` 사용)
  - [ ] 그리드 레이아웃을 3열 유지하되 모든 도시 표시
  - [ ] "전체 도시 순위 보기" 링크 제거
- [ ] CityRankCard 컴포넌트를 CityCard로 리네이밍 및 수정:
  - [ ] `rank`, `rankBadge` props 제거
  - [ ] `city` prop만 받기
  - [ ] 순위 배지 및 순위 관련 UI 제거
  - [ ] 순위별 배경색(`rankBg`) 제거, 일반 카드 스타일로 통일
  - [ ] 나머지 UI는 유지 (좋아요/싫어요, 필터 정보, 태그)
- [ ] `src/data/cities.ts`에서 `TOP_CITIES_THIS_MONTH` export 제거 (더 이상 사용하지 않음)
- [ ] `src/types/city.ts`에서 `CityRank` 인터페이스 제거

### 작업 완료 후 검증 사항
- [ ] 섹션 제목이 "도시 리스트"로 변경되었는지 확인
- [ ] 모든 도시가 좋아요 순으로 정렬되어 표시되는지 확인
- [ ] 각 카드에 순위 뱃지가 없고 동일한 스타일로 표시되는지 확인
- [ ] 좋아요 버튼 클릭 시 실시간으로 정렬 순서가 변경되는지 확인
- [ ] "전체 도시 순위 보기" 링크가 제거되었는지 확인
- [ ] 그리드 레이아웃이 반응형으로 작동하는지 확인 (모바일 1열, 데스크톱 3열)
- [ ] TypeScript 오류 없이 빌드되는지 확인

---

## 전체 작업 완료 후 최종 검증

- [ ] `npm run build` 실행하여 빌드 에러가 없는지 확인
- [ ] `npm run dev`로 개발 서버 실행하여 모든 기능 테스트
- [ ] 홈페이지에 Hero, SocialProof, 도시리스트, 필터 섹션만 표시되는지 확인
- [ ] 필터가 정상 작동하고 조건에 맞는 도시가 필터링되는지 확인
- [ ] 좋아요/싫어요 버튼이 모든 도시 카드에서 정상 작동하는지 확인
- [ ] 도시 리스트가 좋아요 순으로 정렬되는지 확인
- [ ] 모바일/태블릿/데스크톱 반응형 레이아웃 확인
- [ ] 인증 페이지(/login, /register)가 정상 작동하는지 확인

---

## 참고사항

- 각 Phase는 독립적으로 실행 가능하며, 이전 Phase가 완료되어야 다음 Phase로 진행할 수 있습니다.
- 각 단계별로 검증을 완료한 후 다음 단계로 넘어가시기 바랍니다.
- 데이터베이스는 사용하지 않고 가짜 데이터로 진행합니다.
